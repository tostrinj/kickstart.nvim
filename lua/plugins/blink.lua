return {
  {
    'saghen/blink.cmp',
    opts = {
      keymap = { preset = 'enter' }, -- Enter accepts selection
      -- (optional) tweak anything else here; it merges with existing opts
    },
  },
}
